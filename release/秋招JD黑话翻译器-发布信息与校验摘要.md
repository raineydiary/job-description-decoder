# 秋招 JD 黑话翻译器：发布信息与校验摘要

## 建议填写

- 小工具名称：秋招 JD 黑话翻译器
- 简介：把招聘漂亮话翻成人话
- 版本号：1.0.0
- 所需权限：无
- 上传文件：`xhs-autumn-jd-decoder-v1.0.0.zip`
- 图标文件：`xhs-autumn-jd-decoder-icon-v1.png`

## 自动校验结果

- ZIP 约 1.3 MiB，低于 10 MiB 上传上限和 2 MiB 建议值。
- `index.html` 位于 ZIP 根目录；未多套一层文件夹。
- 包内共 10 个文件，仅包含 HTML、CSS、JS、PNG、WOFF2 和 JSON 格式的字体许可证；只有根目录的 `index.html` 一个 HTML 文件。
- 无网络请求、外部运行时资源、iframe、内联脚本、模块脚本、剪贴板 API、定位、Worker、WebSocket、WASM 或其他受限能力。
- JavaScript 语法检查、核心交互回归、LA MER 混合中英文 JD 识别、五张原创角色透明贴纸检查均通过。
- 五个角色采用右侧 3 个、左侧 2 个的错落布局，并分为主／中／小三档；已在 402×874 移动视口检查首屏、结果页与页脚，五个盲盒和说明文字全部位于首屏，所有角色均不遮挡文字或按钮。
- “选个盲盒”箭头每 1.35 秒以阶梯式像素节奏下点两次，已通过多帧位置变化验证；`prefers-reduced-motion` 下自动停用动画。
- 已实际触发盲盒破译及上一条／下一条翻页；五个角色使用不同位移与旋转轨迹，540ms 后均恢复 `idle`，并保留 `prefers-reduced-motion` 静态降级。
- 未声明相册、摄像头、麦克风或本地存储权限。

## 发布前仍需人工确认

- 尚未在小红书 PC 模拟器和真机 WebView 完成最终实测；桌面浏览器通过不等同于真机通过。
- 页面现已替换为本项目生成的原创像素角色：蓝色简历小人，以及绿、粉、黄、紫四个飞行角色；原二创参考图只保留在 `research/`，不进入发布包。
- 已生成独立原创 BB 机图标，1254×1254 PNG、约 1.7 MB，不包含人物或第三方品牌元素。
